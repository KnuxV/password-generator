[project]
name = "password-generator"
version = "0.1.0"
description = "Strong password generator (course exercise)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "zxcvbn>=4.5.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
